# Packages: collection of modules/library
# create a virtual environment(recommended)
# what is a virtual environment?(checkout readme.md) 
# how to create a virtual environment(venv)?(only useful if we use same machine for different projects)
# checkout the readme.md file for this topic.
# just like Github, DockerHub we have PyPi for python packages
# and we can install packages using pip  

# pip install boto3
import boto3
